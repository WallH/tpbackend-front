import { CategoriaDescuentoPipe } from './categoria-descuento.pipe';

describe('CategoriaDescuentoPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoriaDescuentoPipe();
    expect(pipe).toBeTruthy();
  });
});
